using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using Quiz_Matematyczny;

namespace QuizTest
{
    [TestClass]
    public class UnitTest1
    {
        [TestMethod]
        public void TestAdded()
        {

            
            
/**
            Product p1 = new Product { ProductName = "Produkt 1", StockAmount = 2 };
            Product p2 = new Product { ProductName = "Produkt 2", StockAmount = 7 };

            DataContext.AddOrEditProduct(p1);
            DataContext.AddOrEditProduct(p2);

            Assert.AreEqual(DataContext.GetProductList().Count, 2);
            Assert.AreEqual(DataContext.GetProductList()[0], p1);
            Assert.AreEqual(DataContext.GetProductList()[1], p2);
            Assert.AreEqual(DataContext.GetProductList()[0].StockAmount, 2);
            Assert.AreEqual(DataContext.GetProductList()[1].StockAmount, 7);
    */
        }
    }
}
